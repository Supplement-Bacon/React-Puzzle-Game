import styled from "styled-components";

export const Button = styled.button`
    padding: 0.7em 1.5em;
    font-size: 1.4em;
    margin-bottom: 1em;
    cursor: pointer;
    transition: all 0.2s;
    color: black;
    outline: 0;
    border: 0;
    &:hover {
        background: #fff;
        color: #000;
    }
`;
